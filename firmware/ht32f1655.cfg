
if { [info exists CHIPNAME] } {
   set _CHIPNAME $CHIPNAME
} else {
   set _CHIPNAME ht32f1655
}

# CPU TAP
#jtag newtap $_CHIPNAME cpu -irlen 4

# Cortex M3
#set _TARGETNAME $_CHIPNAME.cpu
#target create $_TARGETNAME cortex_m -chain-position $_CHIPNAME.dap

# SRAM: 32K at 0x2000_0000, use first 8K
#$_TARGETNAME configure -work-area-phys 0x20000000 -work-area-size 0x2000

